# Python 3.10 or later.
#
# These are exact pins, not minimums. The Lomb-Scargle power values astropy
# returns shift by about 1e-4 between major versions, which is enough to change
# which of two close peaks wins: under astropy 8.0.1 the USA cluster 2 estimate
# at M4+ becomes 3.832 years instead of 2.216, and Japan cluster 1 at M6+ 1.953
# instead of 1.950. astropy 6.1.7 and 7.2.2 both reproduce the published tables;
# astropy 8 does not. Pin, do not range.
numpy==2.3.3
pandas==2.3.3
scipy==1.16.3
scikit-learn==1.7.2
matplotlib==3.10.7
astropy==7.2.2        # astropy.timeseries.LombScargle -- must stay below 8
nfft==0.1             # non-uniform fast Fourier transform
requests==2.32.3      # used only by code/1_fetch_data.py
openpyxl==3.1.5       # only needed to open the .xlsx files in results/

# Verified to reproduce the published results on three stacks:
#   astropy 6.1.7 (Python 3.10, aarch64), astropy 7.2.2 (x86_64) -- exact
#   astropy 8.0.1 (x86_64) -- two Lomb-Scargle cells differ
